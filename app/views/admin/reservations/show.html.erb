<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>show</title>
</head>
<body>
  <div class="select_all">
    <%= form_with model: @reservation, url: admin_reservation_path, scope: :reservation, action: "post" do |f|%>
      <h2>予約編集ページ</h2>
      <p>選択中の映画ID</p>
      <div class="select">
        <%= @get_movie_id.id %>
      </div>
      <p>スケージュールID</p>
      <div class="select">
        <%= f.text_field :schedule_id %>
      </div>
      <p>選択中の座席</p>
      <div class="select">
        <%= f.text_field :sheet_id %>
      </div>
      <p>氏名</p>
        <%=f.text_field :name %>
      <p>メールアドレス</p>
        <%= f.email_field :email %>
      <div class="select_submit">
      <%= f.submit "編集を確定する"%>
      </div>
    <% end %>
  </div>
</body>
</html>